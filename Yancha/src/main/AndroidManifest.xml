<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.ichigotake.yancha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.context.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/yc_app_name"
        android:theme="@style/YanchaAppTheme" >

        <activity
            android:name=".SplashActivity"
            android:label="yancha">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:label="@string/yc_app_name" >
            <intent-filter >
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="yancha"
                    android:host="login"
                    android:path="/twitter/callback" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChatActivity"
            android:label="@string/yc_app_name" />

        <activity
            android:name=".SearchActivity"
            android:label="@string/yc_app_name"
            android:parentActivityName=".ChatActivity" />

    </application>

</manifest>
